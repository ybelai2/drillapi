services:
  db:
    image: postgres:17
    environment:
      POSTGRES_DB: drill
      POSTGRES_USER: drill
      POSTGRES_PASSWORD: drill
    ports:
      - '127.0.0.1:5432:5432'
    volumes:
      - drill-data:/var/lib/postgresql/data
volumes:
  drill-data:
